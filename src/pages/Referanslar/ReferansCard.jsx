const ReferansCard = ({ index }) => {
  return (
    <div className="cardReferans">
      <img src={`/images/karePost/${index + 1}.png`} alt={"Gumusler Group"} />
    </div>
  );
};

export default ReferansCard;
